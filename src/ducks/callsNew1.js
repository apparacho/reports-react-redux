import {
    getBaseReportDuck,
    getPrefix,
    defaultFilterParams,
    defaultTableParams,
    defaultTablePagination
} from './BaseReport';

const callsDuck = getBaseReportDuck('callsNew1');
export default callsDuck;
